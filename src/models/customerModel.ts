export interface Customer {
  id?: string;
  name: string;
  email: string;
  birthDate: string;
  active: boolean;
  residentialAddress: string;
  businessAddress: string;
  otherAddress: string;
  phone_number: string;
  is_whatsapp: boolean;
  createdAt: string;
  updatedAt: string;
}
